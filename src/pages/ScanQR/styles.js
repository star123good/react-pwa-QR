import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
  gridScan: {
    display: "flex",
    justifyContent: "center",
    marginTop: 50,
  },
  gridTable: {
    padding: 20,
    paddingBottom: 100,
  },
}));
